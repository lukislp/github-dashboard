# Session store for the single-replica (SQLite) setup.
# Not needed when REDIS_URL is set; remove the volume from the Deployment in that case.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: github-dashboard-data
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 256Mi
